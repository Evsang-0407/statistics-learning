# 2026-09-17

- 应用户要求创建了项目级 skill `git-push`（位于 `脚本/.workbuddy/skills/git-push/`），用于一句话触发 statistics-learning 仓库的 commit + push 到 GitHub（origin/main）。工作方式：自动暂存、Conventional Commit 中文消息、拒绝时 pull --rebase、禁止 force push。已通过 skill-creator 校验。
